type Props = {
  activeCourse: any;
  harts: number;
  points: number;
  activeSubcription: boolean;
};

export const UserProgress = ({
  activeCourse,
  harts,
  points,
  activeSubcription,
}: Props) => {
  return <div>UserProgress</div>;
};
